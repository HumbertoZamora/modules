# Agrear: -%> (guion medio -) al interpretar el template no se interŕeten com lineas vacias

<% $syscontact = scope.lookupvar('snmp::syscontact') -%>
<% $community  = scope.lookupvar('snmp::community') -%>
<% $server     = scope.lookupvar('snmp::server') -%>

syscontact  <%= $syscontact %>
rocommunity <%= $community %> <%= $server %>
